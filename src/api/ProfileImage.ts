
export const loadprofilepicture = async ()=>{
    //   load the profile picture from DB
}

export const uploadprofilepicture = async ()=>{
    // upoload the profile picture to backend ( it will store in DB)
}

export const deleteprofilepicture = async ()=>{
    // deletes the profile picture form DB
}